import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aplicacion_Movil_de_Emergencia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
